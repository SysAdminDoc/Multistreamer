{
  "name": "multistreamer",
  "version": "0.10.0",
  "private": true,
  "description": "Self-hosted multi-video streaming viewer with synced room state.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
